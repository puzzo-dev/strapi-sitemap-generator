{
  "collectionName": "components_shared_buttons",
  "info": {
    "displayName": "button"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "variant": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "outline",
        "ghost",
        "link"
      ],
      "default": "primary",
      "required": true
    },
    "size": {
      "type": "enumeration",
      "enum": [
        "sm",
        "lg",
        "md"
      ]
    },
    "icon": {
      "type": "string"
    },
    "iconPosition": {
      "type": "enumeration",
      "enum": [
        "left",
        "right"
      ]
    },
    "buttonLink": {
      "type": "component",
      "repeatable": false,
      "component": "shared.url"
    }
  }
}
